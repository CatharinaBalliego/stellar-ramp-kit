# Server-side — NEVER exposed to the browser
ETHERFUSE_API_KEY="api_sand_..."

# The demo "session": rampkit starts from an approved customer (v0 scope).
# Create/approve one in the Etherfuse sandbox dashboard, then fill these in.
DEMO_CUSTOMER_ID=""
DEMO_PUBLIC_KEY="G..."

# Browser-side demo signer — SANDBOX/TESTNET ONLY. A secret key in the
# browser is acceptable ONLY for this local demo. Never do this in production.
NEXT_PUBLIC_DEMO_WALLET_SECRET="S..."
