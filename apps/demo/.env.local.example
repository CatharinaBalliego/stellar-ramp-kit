# Server-side — NEVER exposed to the browser
ETHERFUSE_API_KEY="api_sand_..."

# Everything below is generated for you: fill in the key above, then run
#   node scripts/bootstrap-sandbox.mjs you@email.com
# It creates the customer + wallet, walks you through the hosted KYC once,
# and rewrites this file with the real values.

# The demo "session": the approved Etherfuse customer the flows act as.
DEMO_CUSTOMER_ID=""
# That customer's Stellar TESTNET wallet address.
DEMO_PUBLIC_KEY="G..."

# Browser-side demo signer — SANDBOX/TESTNET ONLY. A secret key in the
# browser is acceptable ONLY for this local demo. Never do this in production.
NEXT_PUBLIC_DEMO_WALLET_SECRET="S..."
